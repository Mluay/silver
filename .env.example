# Database - use Supabase connection string (Direct / Pooler)
DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres"

# Supabase — استخدم مفتاح anon (public) من: Dashboard → Settings → API → anon public
NEXT_PUBLIC_SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJ... (مفتاح anon فقط)"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Optional: for local .env.local override
# Copy this file to .env and .env.local and fill in values
